# model-training/config.yaml
# Default hyperparameters for training and inference.
# Edit these values to change defaults across all scripts.

# Training defaults
training:
  epochs: 200
  learning_rate: 0.001
  chunk_frames: 8000
  scheduler_patience: 3
  scheduler_factor: 0.1
  velocity_weight: 2.0

# Inference defaults
inference:
  threshold: 0.8
