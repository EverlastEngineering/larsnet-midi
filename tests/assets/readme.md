These are reference files can be used for e2e test runs and other uses.

The matching pairs of files are ground truth, as the midi generated the audio.

However, the MIDI may not match the format and MIDI notes we have, so we can't expect to match 100%.