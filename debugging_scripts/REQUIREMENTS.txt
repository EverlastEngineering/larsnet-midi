# Core requirements (should already be installed in larsnet-midi env)
pandas>=1.3.0
numpy>=1.21.0
scikit-learn>=1.0.0
matplotlib>=3.4.0

# Optional: For Bayesian optimization
scikit-optimize>=0.9.0

# Optional: For enhanced visualizations
seaborn>=0.11.0

# Installation instructions:
#
# Basic (grid search, random search, classifier):
#   These should already be available in your larsnet-midi conda environment
#
# Add Bayesian optimization:
#   pip install scikit-optimize
#
# Add enhanced visualizations:
#   pip install seaborn
#
# Install all optional packages:
#   pip install -r REQUIREMENTS.txt
